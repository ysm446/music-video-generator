# Electron Runner

This directory contains the Electron desktop shell for the app.

## Run

```bash
cd electron
npm install
npm run start
```

Electron starts `../app.py` as a local backend and opens it in a desktop window.
